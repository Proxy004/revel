import React from "react";
import { View, Text } from "react-native";
type findProps = {
  navigation: any;
};
const Find: React.FC<findProps> = ({ navigation }) => {
  return (
    <View>
      <Text>Find</Text>
    </View>
  );
};

export default Find;
